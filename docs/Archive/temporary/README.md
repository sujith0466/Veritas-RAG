# Temporary & Working Notes Archive

This directory catalogs intermediate working notes, temporary fix scripts, and historical investigation logs preserved for full auditability.

## Catalog
- Historical API logs and diagnostic traces (`api_logs.txt`)
- Schema reconciliation scripts (`fix_schema.py`)
- Auth token verification test scripts (`test_jwt.py`)
- Retrieval diagnostic scripts (`scratch/verify_retrieval.py`)
