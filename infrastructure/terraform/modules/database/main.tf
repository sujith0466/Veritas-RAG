# Placeholder for database module
# Cloud-agnostic architecture with AWS as the primary reference implementation.

terraform {
  # Placeholder remote backend configuration for future state management
  # backend "s3" {
  #   bucket = "veritas-rag-terraform-state"
  #   key    = "database/terraform.tfstate"
  #   region = "us-east-1"
  # }
  required_version = ">= 1.0.0"
}
