apiVersion: v1
kind: Secret
metadata:
  name: veritas-rag-secrets
  namespace: veritas-rag-production
  labels:
    app.kubernetes.io/name: veritas-rag
    app.kubernetes.io/component: secrets
    app.kubernetes.io/instance: secret-1
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: kubectl
type: Opaque
stringData:
  # PLACEHOLDER ONLY. Do NOT commit real secrets here.
  # Use AWS Secrets Manager, HashiCorp Vault, or SealedSecrets for production.
  POSTGRES_PASSWORD: "placeholder-password"
  SECRET_KEY: "placeholder-jwt-secret"
  MINIO_ROOT_PASSWORD: "placeholder-minio-secret"
