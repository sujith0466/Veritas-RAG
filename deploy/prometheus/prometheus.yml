global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: "raguard_api"
    metrics_path: "/observability/v1/metrics"
    static_configs:
      - targets: ["api:8000"]
        labels:
          environment: "production"
