"""Vector Storage Foundation module (`ADR-004`).

Provides enterprise-grade, high-throughput vector storage management with HNSW indices,
INT8 scalar quantization, exact multi-tenant payload filters (`ADR-M3-001`), and
asynchronous Celery point indexing workers.
"""
